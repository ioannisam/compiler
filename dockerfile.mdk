FROM debian:bookworm

# install only the MDK tools (mixasm, mixvm)
RUN apt-get update \
 && apt-get install -y --no-install-recommends mdk \
 && rm -rf /var/lib/apt/lists/*

WORKDIR /work

ENV PATH="/usr/bin:${PATH}"